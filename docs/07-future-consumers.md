# Future Consumers

Future OpenTofu and Ansible projects should treat this template as:

| Setting | Value |
|---|---|
| Template VMID | `9024` |
| Template name | `ubuntu-2404-cloud-template` |
| Cloud-init user | `ubuntu` |
| Guest agent | enabled and installed |

Validation status:

- successfully consumed by a separate downstream project
- no additional template-side fixes were required for first use

Do not add OpenTofu files here. Create a separate consumer project later.
